	<article class="container info teaser-title">
		<nav class="pagination" role="navigation">
			{{#get "posts" filter="tags:[hash-member]+id:<{{id}}" limit="1" include="tags" order="published_at desc"}}
			  {{#foreach posts}}
				<a href="{{url}}" class="newer-posts"><span aria-hidden="true">←</span> Newer Posts</a>
			  {{/foreach}}
			{{/get}}
			{{#get "posts" filter="tags:[hash-member]+id:>{{id}}" limit="1" include="tags" order="published_at asc"}}
			  {{#foreach posts}}
				<a class="older-posts" href="{{url}}">Older Posts <span aria-hidden="true">→</span></a>
			  {{/foreach}}
			{{/get}}
		</nav>
	</article>	
